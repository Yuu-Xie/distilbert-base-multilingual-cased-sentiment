{
    "model_checkpoint": "distilbert-base-multilingual-cased",
    "output_dir_name": "distilbert-base-multilingual-cased-sentiment",
    "max_length": 128,
    "learning_rate": 2e-5,
    "per_device_train_batch_size": 128,
    "per_device_eval_batch_size": 256,
    "num_train_epochs": 3,
    "weight_decay": 0.01,
    "warmup_ratio": 0.05,
    "logging_steps": 500,
    "eval_steps": 2500,
    "save_steps": 2500,
    "save_total_limit": 3,
    "early_stopping_patience": 3,
    "seed": 42
}